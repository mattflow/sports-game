// The sibling "guess the 50 states" game. The two games are cross-linked across
// subdomains — set this to the deployed states-game URL per environment.
export const SIBLING_GAME = {
  name: "States Game",
  url: "https://states.mattflow.com",
};
